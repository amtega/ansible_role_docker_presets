# ---
# Fedora 26 image

name: fedora_26
distribution: Fedora
distribution_version: 26
dockerfile: Dockerfile.fedora_26
buildargs:
  http_proxy: "{{ ansible_env.http_proxy | default('') }}"
  https_proxy: "{{ ansible_env.https_proxy | default('') }}"
  dno_proxy: "{{ ansible_env.no_proxy | default('') }}"
