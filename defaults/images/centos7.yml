---
# Centos 7 image

name: centos_7
dockerfile: Dockerfile.centos_7
buildargs:
  user: "{{ docker_presets_ansible_user }}"
  password: "{{ docker_presets_ansible_password }}"
path: "{{ role_path }}/files"
